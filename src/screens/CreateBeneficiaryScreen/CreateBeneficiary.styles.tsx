import {StyleSheet} from 'react-native';
import {APP_COLORS} from '@assets/colors';

export const styles = StyleSheet.create({
  container: {flex: 1, backgroundColor: APP_COLORS.background, padding: 20},
  content: {
    flex: 1,
  },
});
